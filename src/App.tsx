import { Component } from 'solid-js';

const App: Component = () => {
    return (
        <main class="flex min-h-screen flex-col items-center justify-center bg-slate-900 p-8 text-white">
            <h1 class="mb-4 text-4xl font-bold tracking-tight">Needle DAW</h1>
            <p class="text-lg text-slate-400">SolidJS + Tauri Application</p>
        </main>
    );
};

export default App;
